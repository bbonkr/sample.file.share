import { AppOptions } from '../models/AppOptions';

export const appOptions: AppOptions = {
    title: 'Sample File Share',
    subtitle: 'Sample for file share secure',
    github: 'https://github.com/bbonkr/sample.file.share',
    apiVersion: 'v1',
};
